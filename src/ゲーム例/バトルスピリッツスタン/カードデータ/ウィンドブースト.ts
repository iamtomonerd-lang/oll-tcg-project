import { カード } from '../../../データ/カード/カード.js';
import { カード名称データ } from './カード名称データ.js';
import {
  使用フラッシュ,
  自分のスピリットを選ぶ,
  それのBPを変える,
  このターン中,
} from '../効果部品/index.js';

// ウィンドブースト（マジック）
// 調査カード16番のテキストをそのままデータにしたもの。
// ［フラッシュ］自分のスピリット1体を指定する。このターン中、それをBP+3000する。
export const ウィンドブースト名称: Omit<カード名称データ, '識別子'> = {
  表示名: 'ウィンドブースト',
  説明: '追い風が牙を押し出す。',
  カード名: 'ウィンドブースト',
  カードナンバー: '26RSD01-020',
  カード種別: 'マジック',
  レアリティ: 'C',
  コスト: 2,
  系統: [],
  ブロックアイコン: '',
  属性: ['赤'],
  軽減シンボル: ['赤'],
  カードテキスト: '［フラッシュ］自分のスピリット1体を指定する。このターン中、それをBP+3000する。',
  効果: [
    {
      ...使用フラッシュ,
      処理: [自分のスピリットを選ぶ(), それのBPを変える(3000, このターン中)],
    },
  ],
};

export function ウィンドブーストを作成(識別子: string): カード {
  return new カード(識別子, { 識別子, ...ウィンドブースト名称 });
}
