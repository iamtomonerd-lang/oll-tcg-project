import { ゾーン } from '../../../データ/ゾーン/ゾーン.js';

// 破棄判定（ルール層 ＝ 状態を返すだけ）
export class 破棄判定 {
  // 破棄はカードの持ち主の手札またはデッキから行える
  破棄可能な領域か(領域: ゾーン): boolean {
    const キー = 領域.名称.識別子;
    return キー === '手札' || キー === 'デッキエリア';
  }
}
