// ソウルマジック判定（ルール層 ＝ 状態を返すだけ）
// 《ソウルマジック》：指定されたシンボルが自分のフィールドにあれば、ソウルコアのみで
// コストを支払って使用できる共通効果（12-2-1-2-1）。
export class ソウルマジック判定 {
  発揮可能か(指定シンボルが自分のフィールドにあるか: boolean, ソウルコアあり: boolean): boolean {
    return 指定シンボルが自分のフィールドにあるか && ソウルコアあり;
  }
}
