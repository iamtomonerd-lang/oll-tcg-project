// ⑤ 代償（▶の左側）
//
// 【起動】効果の「▶」より左、効果を撃つために先に払うもの。
// 払えなければ効果そのものが撃てない。
//
// 効果定義の コスト: に渡す。中身は処理ステップの並びなので、
// ④何をする の部品をそのまま使える。ここはその「よく使う組み合わせ」を名前にしたもの。

import { 処理ステップ } from '../../../効果解釈/効果定義.js';
import { 自分の手札から選ぶ, 選び方 } from './誰を.js';
import {
  このカードを疲労させる as 疲労させる,
  このカードを破壊する as 破壊する,
  それを破棄する,
} from './何をする.js';

// 「このネクサスを疲労させる▶」
export const このカードを疲労させる: 処理ステップ[] = [疲労させる];

// 「このネクサスを破壊する▶」
export const このカードを破壊する: 処理ステップ[] = [破壊する];

// 「自分の手札の系統：「風牙」を持つカード1枚を破棄する▶」
export function 手札のカードを破棄する(絞り込み: 選び方 = {}): 処理ステップ[] {
  return [自分の手札から選ぶ(絞り込み), それを破棄する];
}
