export class スピリット {
  readonly 識別子 = 'スピリット';
  readonly 表示名 = 'スピリット';

  // スピリットが持つルール：
  // - アタック判定／ブロック判定：アタック/ブロック可能判定、召喚直後アタック可能
  // - Lv判定：Lv計算、Lvアップ/ダウン判定、真界放対応
  // - BP判定：現在のLvに対応したBP計算
  // - 消滅判定：Lv1コスト未満での消滅判定
}
