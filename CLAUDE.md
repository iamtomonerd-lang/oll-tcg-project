# このプロジェクトについて

バトルスピリッツ（スタン環境）を実装したもの。収録は `26RSD01 バトスピエントリーデッキ 赫焔ノ風牙`。

**ゲームはブラウザの中だけで完結する。** サーバーは無くても遊べる。

---

## 返答のしかた

**日本語で返答する。** コード内の名前もコメントも日本語で書く。

---

## いちばん大事な決まり

### 1. 名前は日本語で書く

クラス・関数・変数・ファイル名、すべて日本語。英語との混在はしない。

```typescript
export function 相手のスピリットを選ぶ(設定: 選び方 = {}): 処理ステップ {
  return 選ぶ({ 場: '相手', 種別: 'スピリット' }, 設定);
}
```

### 2. カードの効果はクラスではなくデータで書く

カード1枚ごとにクラスを作らない。**部品の組み合わせ**で書き、`効果インタプリタ` が解釈する。

```typescript
効果: [
  {
    Lv: [1, 2],
    ...召喚時,
    処理: [相手のスピリットを選ぶ({ BP以下: 7000 }), それを破壊する],
  },
],
```

部品は6分類。置き場所は `src/ゲーム例/バトルスピリッツスタン/効果部品/`。

| ファイル | 中身 | 例 |
|---|---|---|
| `いつ.ts` | ①きっかけ | 召喚時／アタック時／起動フラッシュ |
| `条件.ts` | ②〜なら | 真界放している／疲労している味方がいる |
| `誰を.ts` | ③対象の絞り込み | 相手のスピリットを選ぶ({ BP以下: 7000 }) |
| `何をする.ts` | ④効果の本体 | それを破壊する／トラッシュのコアを置く |
| `代償.ts` | ⑤▶の左側 | 代償.手札のカードを破棄する({ 系統: '風牙' }) |
| `長さ.ts` | ⑥効いている長さ | このバトル中／このターン中 |

対応表は `docs/効果パーツ一覧.md`。**カードを1枚足すたびに、この表の使用実績も更新する。**

### 3. 効果の正解は「語彙調査」にある

`src/ゲーム例/バトルスピリッツスタン/効果語彙/調査カード23枚.ts` に、
カードテキストを書き下した効果定義がある。新しいカードを実装するときは：

1. まずこのファイルから同じテキストの効果（`カードN`）を探す
2. 部品を組み合わせて、**同じものが出ることを `deepEqual` で固定する**

```typescript
assert.deepEqual(効果一覧[0], カード20);
```

ここがずれたら、部品化のどこかで意味が変わっている。

---

## カードを1枚足すときの手順

1. `public/cards/<カードナンバー>.png` の画像を**実際に読んで**、数値とテキストを確かめる
   - 特に**シンボルが通常かEXか**は画像でしか判断できない。
     宝玉に金の装飾が付いていれば EX、無ければ通常。
     《継召》持ちはEXシンボルを「食べる側」なので、通常であることが多い。
2. `調査カード23枚.ts` から同じ効果を探す
3. `カードデータ/<カード名>.ts` を作る（既存のカードを手本にする）
4. `カードデータ/<カード名>.test.ts` を作る
   - カード情報・Lv・シンボル・キーワード
   - **語彙調査との `deepEqual`**
   - 効果を通しで動かす（境目の数値、条件を満たさない場合、Lv違いなど）
5. 登録する
   - `カードデータ/カード帳.ts` … **必須**。ここに入れないとデッキ構築の画面に出ない
   - `public/game.js` … `CARD_ART`（絵柄がある場合）
   - お試し用のデッキが要るなら `src/アプリ/ゲームAPI.ts` にデッキタイプを足し、
     `public/game.js` の `deckNames`・`public/index.html` のボタン・
     `src/サーバー/ウェブサーバー.test.ts` のデッキ一覧も揃える
     （デッキ構築で組めるので、必須ではない）
6. `npm test` を流す（全部通ること）
7. `docs/効果パーツ一覧.md` の使用実績を更新する
8. 画面で確かめる（下の「動かして確かめる」）

### 新しい部品が要るとき

既存の部品で書けないときだけ、部品を1つ足す。足したら `docs/効果パーツ一覧.md` に1行追記する。

---

## 作りの全体像

```
public/index.html … 画面。game.js と layout.js を読む
public/game.js    … 盤面の描画と操作。fetch('/api/...') で問い合わせる
public/ゲーム本体.js … esbuild でまとめたもの（ビルド生成物・gitに入れない）

src/ブラウザ/起動.ts   … fetch を包んで /api/ を画面内で受ける（ブラウザ用の入口）
src/サーバー/ウェブサーバー.ts … 同じものをHTTPで包む（手元で動かすとき用の入口）
src/アプリ/ゲームAPI.ts … ★ゲームのAPI本体。土台に依存しない
src/ゲーム例/バトルスピリッツスタン/ … ルール・カード・効果
src/効果解釈/ … 効果インタプリタ
```

**ゲームの決まりごとは `src/アプリ/ゲームAPI.ts` にしかない。**
ブラウザから来てもHTTPから来ても同じ道を通るので、片方だけ挙動がずれることがない。
サーバー側に処理を書き足さないこと。

---

## デッキ構築

デッキの決まりは **ちょうど40枚・同じカード名は3枚まで**。

| 置き場所 | 役割 |
|---|---|
| `カードデータ/カード帳.ts` | 実装済みカードの索引。**カードを足したらここに1行** |
| `判定/デッキ構築判定.ts` | 40枚・同名3枚の判定 |
| `public/デッキ構築.js` | 組む画面 |

**同名はカード名で数える。カードナンバーではない。**
別の番号で同じ名前が出たとき、番号で数えると4枚以上入ってしまう。

カード帳は作成関数だけを持ち、カードナンバーや名前は
実際に1枚作って ルール に聞く。カードデータ側と二重に持たないので表記がずれない。

画面は「あと何枚足せるか」でボタンを止めるだけで、組めるかを自分で決めない。
最後は `/api/game/start` が `デッキ構築判定` で弾く（画面を信用しない）。

エンジン検証用のデッキ（同じカード40枚など）は同名の上限を超えている。
対戦で使える構築ではないが、効果を通すためのものなのでそのままにしてある。

## セーブデータ（コピペで持ち運ぶ）

ブラウザに残したものは Safari のデータを消すと失われるので、
1本の文字列に書き出せるようにしてある。実装は `public/セーブデータ.js`。

```
BSSAVE1.<中身のJSONをbase64にしたもの>.<検査値>
```

改行が混ざっても読める（貼り付け時に空白を落とす）。
途中で切れていれば検査値が合わず、その旨を伝えて弾く。

### 残すものを増やすとき

**`セーブデータ.js` も文字列の形も変えなくてよい。**
その機能の側から名乗り出るだけ。

```js
window.セーブデータ.登録する({
  鍵: 'デッキ',
  名前: '組んだデッキ',
  集める: () => 書き出したい中身,      // 無ければ null を返す
  受け取る: 中身 => { 読み込んだときの処理 },
  要約: 中身 => `${中身.length}個`,    // 省略可。書き出し画面に出す短い説明
});
```

登録は読み込み時に行う。`index.html` で **`セーブデータ.js` を先に置くこと**
（`layout.js` がその例）。

知らない鍵が入っていても黙って飛ばすので、
先の版で増えた項目が混ざっていても読める分だけ読める。

### 想定している追加先

デッキ構築・リプレイ・実績。いずれも上の3つ（集める／受け取る／要約）を書くだけで載る。

---

## 動かして確かめる

```bash
npm test              # 全テスト
npm run build:all     # TypeScript と ブラウザ用のまとめ、両方
npm run server        # http://localhost:3000 で開く
```

画面の確認は Playwright を使う（Chromium は `/opt/pw-browsers/` に入っている）。

**ただし Playwright の `click()` は要素を自動でスクロールして画面内に入れてから押す。**
そのため「開いたパネルが画面の外にある」不具合を素通りさせる。実際にそれで
「押しても何も起きない」を見逃した（パネルが1画面ぶん下に開いていた）。

画面に何かを足したときは、次の2つを守って確かめること。

- 押すのは `page.evaluate(() => 要素.click())`（自動スクロールなし＝指でタップしたのと同じ）
- `getBoundingClientRect()` で**本当に画面の中にあるか**を確かめる

そして**起動直後の状態から**、利用者が押せるものだけを辿ること。
検証の都合で近道すると、入口が隠れていても気づけない
（`deckBack` を押して近道したせいで、デッキ構築の入口が隠れているのを見逃した）。

手で確かめてほしい項目は `docs/iPadでの確認項目.md` にある。

**サーバー無しでも動くことを確かめたいときは**、`public/` をただ配るだけの
静的サーバーを立てて開く。`/api/` へのネットワーク通信が 0 件なら、
ブラウザの中だけで動いている。

---

## 版の印（反映されたか確かめられるようにする）

公開したものが本当に届いたかを、コードを読まずに確かめられるようにしてある。

`scripts/ブラウザ用にまとめる.mjs` がビルド時に2つ作る。

| 作るもの | 役割 |
|---|---|
| `public/ゲーム本体.js` に埋め込む `__版__` | **今動いているコード**が自分で名乗る印 |
| `public/版.json` | **今公開されているもの**を知らせる札 |

`public/版表示.js` がこの2つを見比べる。食い違えば「新しい版があります」と出す。
どちらもビルド生成物なので git には入れない。

印は「短いSHA・日時・コミットの件名」。**件名を出すのが肝**で、
SHAより「何が入ったか」のほうが人には確かめやすい。

- 画面左下に常時表示（盤面が出ている間は引っ込む）
- 押すとその場で最新かどうかを確かめる
- 手元で未コミットのままビルドすると印の末尾に `+` が付く

**`版.json` はサービスワーカーで絶対にキャッシュしないこと。**
ここを手元の保存分で答えると、更新が出ていても永久に気づけなくなる。

### GitHub Pages は全ファイルに `max-age=600` を付ける

```
/index.html        cache-control: max-age=600
/ゲーム本体.js      cache-control: max-age=600
```

**`版.json` だけ `no-store` にしても足りない。** 帯は正しく出るのに、読み直したとき
戻ってくる `index.html` と `ゲーム本体.js` はブラウザの控えのまま——という食い違いが起きる。
実際それで「更新するボタンは出るのに更新されない」が出た。

対策は3段構えで入れてある。

| どこ | 何をしているか |
|---|---|
| `sw.js` | `fetch(要求, { cache: 'no-cache' })` で毎回サーバーに確かめる（変わっていなければ304） |
| `版表示.js` | 押されたら `cache: 'reload'` で読み込んでいるファイルを取り直してから読み直す |
| `版表示.js` | 新しいサービスワーカーの交代を待ってから読み直す（待たないと1回目が空振りする） |

**手元のふつうの静的サーバーは `max-age` を付けないので、この不具合は再現しない。**
確かめるときは `node scripts/更新が届くか確かめる.mjs`（本番と同じヘッダを付けて配る）。

## 公開

`main` に push すると GitHub Actions が GitHub Pages に置く（`.github/workflows/公開.yml`）。
テストが通らなければ公開しない。

**push だけに頼らないこと。** GitHubアプリのトークンでマージすると、
その push ではワークフローが起動しない。実際にそれで公開が止まり、
気づかないまま古い版が残ったことがある。

そのため10分おきの見回りを入れてある。`main` の先頭と、
前回 github-pages 環境へ公開したコミットを見比べ、違えば公開する。
同じなら何もしない（`check` ジョブで止まる）ので、空振りは軽い。

**ただし見回りは保険であって、当てにしてはいけない。**
GitHubの定期実行は best effort で、混んでいると大きく遅れるか飛ばされる。
実際に `*/10` を入れたのに50分以上走らなかったことがある。

**マージしたら Actions → 「公開」 → `Run workflow` で自分で起こすこと。**
そのうえで `版.json` を見て、`印` が `main` の先頭と一致するまで確認する。
「マージしたから届いたはず」で終わらせない。

変更が無くても出しなおしたいときは `force` を on にする。

なお**60日間リポジトリに動きが無いと、GitHubは定期実行を止める**。
長く放置したあとは手動で起動しなおすこと。

### 失敗した公開を「公開済み」と数えない

見回りの判定は「`main` の先頭」と「前回この環境へ公開したコミット」を見比べる。
ここで `deployments` を1件見るだけにすると、**配置に失敗しても記録は残る**ため、
その失敗を「公開済み」と数えてしまう。

一度そうなると、以後の公開はすべて「変わっていない」と判断されて丸ごと飛ばされる。
このとき**実行結果は success のまま**で、サイトだけが古い版に留まる。
いちばん気づけない止まり方で、実際に 1c90860 でそうなった
（push で起動 → 組み立ては成功 → Pagesへの配置がタイムアウトで失敗 →
以後の `Run workflow` はすべてスキップ、しかも success）。

いまは新しい順に何件か辿り、**状態が `success` のものだけ**を公開済みと数える。

**「実行結果が success」と「サイトが新しくなった」は別物。**
最後は必ず `版.json` の `印` を見ること。

置き場所は `https://<ユーザー名>.github.io/<リポジトリ名>/` で**根ではない**。
そのため `public/` の中のパスは**すべて相対**で書くこと。
`/icon-192.png` のような絶対パスを書くと本番だけ壊れる。

例外は `fetch('/api/...')` だけ。これはネットワークに出ず、
`src/ブラウザ/起動.ts` が受け止めるので絶対パスのままでよい。

---

## テストを書くときに引っかかりやすいところ

- `準備を行う` はシャッフルと初期ドローまでやる。**デッキや手札を積むのはその後**
- 先攻第1ターンはアタックステップを飛ばす。アタックを試すなら1巡進める
- `2回目` のような数字始まりはJSの識別子にできない
- 対戦テストは種（seed）を渡すと同じ試合が再現できる（`new 試合(種)`）
- `npm run clean` を挟まないと、消したファイルの古い `dist` が残って幽霊テストが動くことがある

---

## これまでに直した土台の不具合（同じ轍を踏まないために）

新しいカードを1枚足すたびに、それまで誰も通っていなかった道が見つかっている。
**カードのテストが落ちたら、まずカードではなく土台を疑う。**

| 症状 | 原因 |
|---|---|
| ソウルコアが勝手に動く | `除外: 'ソウルコア'` が実際には見られていなかった |
| メインステップで撃てるはずのない起動効果が撃てる | `トリガー: '自分のアタックステップ'` が起動効果では評価されていなかった |
| マジックの片方の効果が両方発動する | 使用効果がタイミングで絞られていなかった |
| プレイヤーが選んだ順番が無視される | 答えを `候補.filter()` で受けていて並び順が捨てられていた |
| 払えない代償の効果が撃ててしまい、〔ターン1回〕だけ失う | 事前判定が自分自身を対象とするコストしか見ていなかった |
| テストが4回に1回落ちる | `デッキ管理.シャッフルする` が `Math.random()` を直に使っていた |
| 効果で破壊した相手が場に残り続ける（対象は選べるのに壊れない） | 破壊の第2段階を呼んでいたのが バトル管理（BP比べ）だけだった |
| 消滅したのに軽減シンボルが1つ満たされたまま | 消滅も第2段階を誰も呼んでいなかった。画面が隠していたので「消えたように見える」だけだった |
| 最奥：風牙岩Lv2がバトルで相手を倒しても反応しない | 『自分が相手のスピリットを破壊した時』をBP比べから知らせていなかった |
| アタックステップに入っただけで【起動：フラッシュ】が撃てる | 画面に出す起動効果をタイミングで絞っていなかった（全カード共通） |
| ブロックしていないのに「ブロック後」の割り込みの窓が開く | ブロッカーの有無に関わらず窓を開けていた（8-1-4） |
| 起動効果を撃たずに見送れない | 起動効果のパネルが割り込みパネルの「何もしない」を覆っていた（狭い画面でだけ起きる） |
| 《継召》が一度も発揮できない | 判定も管理もあるのに、呼び出す者がどこにもいなかった（`継召管理.発揮する` の呼び出し箇所0件） |
| 《ソウルマジック》が発動しない | 同上。`ソウルマジック管理` は生成されるだけで使われていなかった |
| カードの上のコアでコストを払えない | `ゲームAPI` が支払い元をリザーブに決め打ちしていた |
| ソウルコアを狙って払えない | `プレイコスト管理` が「通常コア優先」で固定だった。トラッシュにソウルコアを作れず、キュペルLv2を試せなかった |
| どのカードでも《継召》が使えてしまう | `継召判定` が発揮する側のキーワードを見ていなかった。軽減シンボルさえあれば食べられた |
| 《ソウルマジック》がやはり撃てない | 手札を押せる条件が「コアが足りるか」だけだった。コストを飛ばす撃ち方なので、どの条件も満たさない |
| 支払い方を自分で選べない | 画面が `window.設定.毎回支払いを選ぶ` を見ていたが、その `設定` を作る者がいなかった |
| ソウルコアと通常コアを入れ替えられない | `コア管理.ソウルコアと通常コアを交換` の呼び出し箇所がテストだけだった |
| ロワミークを選んでもグン＝ガタが始まる | `/api/game/start` の許可リストが手書きで、`rowamique` が漏れていた。知らない名前を黙って差し替えていた |
| 支払い画面と割り込みの窓が完全に重なる | popover が9枚とも画面中央・同じz-index。2枚だけ直して残りを放っておいた |
| ［フラッシュ］のマジックがメインステップで使えない | 撃ち方を `割り込み中か ? 'フラッシュ' : 'メイン'` と決め打ちしていた。［フラッシュ］しか持たない札はメインで何も見つからず静かに終わる。**［フラッシュ］の効果はメインステップでも使える** |

### 「実装済み」に見えて、誰も呼んでいないことがある

判定（ルール層）と管理（エンジン層）が揃っていても、**入口から繋がっていなければ動かない。**
《継召》《ソウルマジック》《ソウルコアと通常コアの交換》はこれで長く気づかれなかった。
クラスもテストもあるのに、`ゲームAPI` が呼ばず、画面にも入口が無かった。

新しい仕組みを足したら、`grep` で**呼び出し箇所が1件以上あること**を確かめること。
`src/**/*.test.ts` しか呼んでいないなら、それは本番では動いていない。

### 名簿は1か所にまとめる（黙って別のものに差し替えない）

デッキの名前は「型の宣言」と「`/api/game/start` の許可リスト」に**二重に手書き**されていた。
名簿に足しても許可リストに書き忘れると、知らない名前として黙ってグン＝ガタに差し替わる。
実際にロワミークがこれで漏れ、選んでもグン＝ガタの試合が始まっていた。

今は `デッキタイプ一覧` が唯一の名簿で、型も入口の検査もそこから作る。
**知らない名前は断る。** 黙って別のものに差し替えると、
「選んだものと違うものが動いている」ことに誰も気づけない。

### パネルは常に1枚（popover はどれも画面のまん中）

popover は全部 `position: fixed; left:50%; top:50%` で z-index も同じ。
**2枚出れば必ず重なる。** 起動効果と割り込みの2枚だけ直して残りを放っておいたため、
フラッシュ中に支払い画面を開くと窓とぴったり重なって手が進められなくなった。

`game.js` の パネルを開く／パネルを閉じる を通すこと。
人が開くパネルは同時に1枚だけで、開いているあいだは盤面のパネルが引っ込む。

**確かめるときは `offsetParent` を使わないこと。** `position: fixed` の要素では
常に null になるので、「パネルが出ていない」と誤って読む（実際にそれで空振りした）。
`hidden` と `getBoundingClientRect()` の幅・高さで見る。

**中のボタンの `hidden` も当てにしないこと。** パネルを閉じても、
その中のボタンの `hidden` は false のまま（大きさが 0×0 になるだけ）。
これで2回はまった —— 閉じている「ブロックしない」を800回押し続け、
閉じている効果パネルのボタンを755回押した。どちらも**アプリは正しく、確かめ方が誤っていた**。
押す前に `getBoundingClientRect()` の幅・高さが0でないことを見ること。

### 破壊は2段階であることに注意する

```
第1段階 破壊管理.破壊する        … 待機状態にする（ここで『破壊時』を知らせる）
第2段階 破壊管理.トラッシュへ置く … 場から消してコアをリザーブへ戻す
```

**カードのテストで「破壊されたか」を見るときは、第1段階（`待機状態か`）ではなく
第2段階まで、つまり「場から消えてトラッシュにあるか」を見ること。**
第1段階しか見ていなかったせいで、実機で壊れないカードを589本のテストが素通りさせた。

効果からの第2段階は `効果実行器.キューを進める` の最後で行う。
解決の途中で呼ぶと『破壊時』を知らせる前に場から消えてしまうので、キューを空にしてから呼ぶ。

消滅（コアがLv1コスト未満）も同じ2段階で、こちらは `試合.ルール処理を実行する` が片付ける。

`ウェブサーバー.test.ts` の `不変条件を検査する` が、
自動対戦の全手番で「破壊・消滅したカードが場に残っていないか」を見張っている。

### テストが素通りさせる3つの型

同じ形の不具合を4回通してしまった。落とし穴はいつも同じ3つ。

| 型 | 何が起きるか | 書き方 |
|---|---|---|
| 途中までしか見ない | 「待機状態になった」で止めると、場に残り続けても通る | **盤面に見えるもの**を見る（場から消えたか・トラッシュにあるか・コアが戻ったか） |
| 事象を手で鳴らす | `事象を通知する(...)` を直に呼ぶと、本番で誰もそれを鳴らしていなくても通る | **本物の経路**を通す（`アタック宣言する` → `バトルを解決する`、`召喚する`、ステップ進行） |
| 実戦では起こらない盤面を作る | コアを乗せずに場へ置くと、消滅判定に引っかかる不自然な状態になる | `場に出す` はLv1を保てる最小限のコアを乗せる |

画面側で「おかしな状態を隠す」のもやってはいけない。
消滅したカードを描かない応急処置が入っていたせいで、
中身では場に残っているのに「消えている」ように見え、発見が遅れた。

---

## AI（対戦相手）

`src/ゲーム例/バトルスピリッツスタン/AI/` にある。詳しくは `docs/AIを強くする.md`。

| ファイル | 役割 |
|---|---|
| `対戦相手.ts` | AIが満たすべき口。進行させる側はこれだけを見る |
| `打てる手.ts` | いま何ができるかを**全部並べる**（挙げるだけ。良し悪しは決めない） |
| `盤面評価.ts` | 盤面を1つの数字にする。**強さの正体はこの重み** |
| `思考AI.ts` | 打てる手を**複製の上で実際に打って**測り、比べて選ぶ。vsAI で使われている |
| `練習用AI.ts` | 古い相手（手札の順に出すだけ）。強さを測るときの物差しとして残してある |
| `腕試し.ts` | AI同士を何百試合も戦わせて勝率を出す |
| `学習.ts` | 自己対戦で重みを鍛える（山登り） |
| `学習した重み.ts` | 鍛えた結果。**手で書き換えない**（`npm run 学習` で作り直す） |

**強さは 思考AI ではなく 重み の側にある。** 学習は重みを鍛えることなので、
判断の骨組み（思考AI）に強さを混ぜないこと。反映先が2か所に散る。

**「強くなった」は必ず `腕試し` の数字で言う。** 先攻は有利なので、
先攻・後攻を入れ替えて半分ずつ戦わせること（`腕試しする` がそうしている）。
入れ替えないと「強いAI」ではなく「先攻」を測ってしまう。

同じAI同士で五分になることも一緒に見る。ここが崩れていたら、
その勝率は強さではなく測り方を見ている。

**思い込みで重みを動かさない。** 「［フラッシュ］のマジックは取っておくほうが得」と
考えて割引を入れたが、500試合ずつ測ったら**得にならなかった**（51.8% → 49.4%）。
値を変えるなら測ってから。

### 学習（`npm run 学習`）

自己対戦で重みを鍛え、`AI/学習した重み.ts` に書き出す。ビルドに含まれるので
**ブラウザ側は何も変えなくてよい**（サーバーも要らないまま）。40世代でおよそ9分。

- **`評価の重み` に項目を足したら、学習を回し直すこと。**
  焼き込んだ側に無い項目は既定値に落ちるだけで、誰も気づけない
  （テスト「焼き込んだ重みは、いまの評価の重みと同じ項目を持つ」で止めている）
- 学習の最後に**学習に使っていない種**で1000試合測り直し、その数字を
  `学習した重み.ts` のコメントに残している。ここが50%を大きく超えていなければ採用しない
- 手を入れるのは重みだけ。**思考AI（判断の骨組み）には触らない**

### 盤面の複製（先読みの土台）

`試合.複製する(種)` が同じ盤面の別物を作る。AIはこれを使い、
手を「見込み」ではなく**打ってみて測る**。1回 0.06ms ほど。

写すのは盤面（プレイヤー・ゾーン・カード。**コアはその中に入っている**）と、
状態を持つ7つ（一時効果・常在効果・ターン履歴・誘発型・置換型・制限効果・効果実行器）。

**状態を持つ管理を新しく足したら、`複製する` に1行足すこと。**
写し忘れても例外は出ない。複製が静かに嘘の盤面を答えるだけなので、
「なんとなく弱い」としか見えず、いちばん見つけにくい。

カードを指している記録は**複製したカードに差し替える**。
本物を指したままだと、複製をいじったつもりが本物に漏れる。

効果の答え待ちの最中は複製できない（例外を投げる）。

### 「テストが通った」と「テストが働いている」は別物

複製のテストを書いて通ったので確かめたら、
**写しの呼び出しを7つ全部消してもテストは通った**。
途中まで進めただけの盤面では、どの管理も中身が空だったため。
「複製できている」ではなく「何も入っていない」を見ていた。

**守りを入れたら、守りを外して落ちることまで確かめる。**

### 重みを測るときは「同じ重みどうし」もものさしに取る

重みを変えて 53.2% が出て「差がある」と読みかけたが、
同じ重みどうしを測ったら 50% 前後で、53.2% はただのばらつきだった。
1回の比較だけで決めず、**種の範囲を変えて何度か測る**こと。

### 自己対戦の進行役が、本番と同じ場面を作れているか疑う

`対戦進行.ts` が**割り込みの窓を一度も開いていなかった**。
風牙の起動効果は両方とも【起動：フラッシュ】なので、
40試合で起動効果0回という数字が出ていた。
**AIが撃たないのではなく、撃てる場面を作っていなかった。**

学習は自己対戦で回すので、こうなるとフラッシュの使い方を一切学べない。
**測り方の穴が、そのまま強さの穴になる。**

### 勝率が良くても、1試合を1手ずつ見ること

練習用AIに87%勝っていたが、実戦を1手ずつ見たら
Lvアップを245ターンで9回しかせず、場のスピリットの96%がLv1のままだった。
物差し（練習用AI）も同じ穴を持っていると、穴は数字に出てこない。

### 種を1つに決め打ちしたテストは、AIを変えると落ちる

AIを強くしたら、種11でその場面に入れなくなってテストが落ちた。
確かめたいのが「その場面が来たら正しく動くこと」なら、
**場面が現れるまで種を変えて探す**こと。

## 用語

| 語 | 意味 |
|---|---|
| 《真界放》 | Lv2コストを払わなくてもソウルコア1個でLv2になれる（12-2-1-1-2）。Lv2にいるとき真界放状態（12-2-1-1-3） |
| 《継召》 | トラッシュのEXシンボルを除外して軽減できる（12-2-1-3） |
| 《ソウルマジック》 | 自分の該当色シンボルがあればソウルコアだけで使用できる |
| 〔ターン1回〕 | カードのインスタンスごとに1ターン1回（12-3-1） |
