{
  "name": "oll-tcg-project",
  "version": "0.1.0",
  "description": "A universal foundation for creating Trading Card Games",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "typecheck:browser": "tsc -p tsconfig.ブラウザ.json",
    "build:browser": "npm run typecheck:browser && node scripts/ブラウザ用にまとめる.mjs",
    "build:all": "npm run build && npm run build:browser",
    "dev": "tsc --watch",
    "test": "npm run build && node --test \"dist/**/*.test.js\"",
    "lint": "eslint src",
    "clean": "rm -rf dist public/ゲーム本体.js public/版.json",
    "server": "npm run build:all && node dist/サーバー/ウェブサーバー.js",
    "学習": "npm run build && node scripts/AIを鍛える.mjs"
  },
  "keywords": [
    "tcg",
    "trading-card-game",
    "card-game",
    "game-engine"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.0",
    "@types/node": "^20.0.0",
    "esbuild": "^0.28.1",
    "typescript": "^5.0.0"
  }
}
